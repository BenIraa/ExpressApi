// import {contacts} from '../Controller/contacts.controller.js';

// const contactsValidator = (req, res, next) => {
//     let {firstname, lastname, email, telephone, message} = req.body
//     if(!firstname) return res.json({error: 'firstname Missing'});
//     if(!lastname) return res.json({error: 'lastname Missing'});
//     if(!email) return res.json({error: 'email Missing'});
//     if(!telephone) return res.json({error: 'telephone Missing'});
//     if(!message) return res.json({error: 'message Missing'});
//     else next();

// }
// export {contactsValidator as default}